Can ML-DSA be integrated into the trusted TDX quote generation flow while preserving
    the original security model?

What overhead does ML-DSA introduce compared with ECDSA in quote generation and
    verification?

What parts of the full attestation workflow can be validated in simulation, and what
    remains blocked until real platform support is available? 